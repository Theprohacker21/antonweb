{
  "version": 2,
  "builds": [
    { "src": "api/**/*.js", "use": "@vercel/node" },
    { "src": "public/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1.js" },
    { "src": "^/(.*\\.(css|js|png|jpg|svg|ico|json))$", "dest": "/public/$1" },
    { "src": "/.*", "dest": "/public/index.html" }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}

